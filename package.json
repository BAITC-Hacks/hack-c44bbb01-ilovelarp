{
  "name": "hackalem-business-task-catalog",
  "version": "0.1.0",
  "description": "AI Sana hackathon MVP: business task gamification, rating, catalog, team proposals. No build step — open index.html directly, or serve statically.",
  "scripts": {
    "test": "node test/smoke.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "jsdom": "^30.1.1"
  }
}
